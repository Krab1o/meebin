package auth

import (
	"context"

	smodel "github.com/Krab1o/meebin/internal/model/s_model"
)

func (s *authService) Login(ctx context.Context, id uint64) (*smodel.Tokens, error) {
	return nil, nil
}
